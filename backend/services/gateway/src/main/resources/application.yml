spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:9098/realms/projet_team3"
  config:
    import: configserver:http://localhost:8888
  application:
    name: gateway-service
